import { YouTubeSearch } from "./search";
export declare class YouTubeAudioV2 extends YouTubeSearch {
    private dir;
    private useBuffer;
    private fname;
    constructor(url: string);
    outputFile(dir: string): YouTubeAudioV2;
    outputBuffer(): YouTubeAudioV2;
    filename(fname: string): YouTubeAudioV2;
    download(): Promise<any>;
}
