import { YouTubeSearch } from "./search";
export declare class YouTubeAudio extends YouTubeSearch {
    private audioCodec;
    private audioChannels;
    private audioBitrate;
    private fname;
    private ext;
    private dir;
    constructor(url: string);
    codec(audioCodec: string): YouTubeAudio;
    bitrate(audioBitrate: string): YouTubeAudio;
    channels(audioChannels: number): YouTubeAudio;
    outdir(dir: string): YouTubeAudio;
    extension(ext: string): YouTubeAudio;
    filename(fname: string): YouTubeAudio;
    download(): Promise<any>;
}
